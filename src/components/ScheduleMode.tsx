import { Sequencer } from "@kzkymur/sequencer";
// import { useSettingsStore } from '../store/modeStore';

export const ScheduleMode = () => {
  // const { settings, setSettings } = useSettingsStore();

  return (
    <div className="timer-controls">
    </div>
  );
};